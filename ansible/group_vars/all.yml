---
# Shared cluster variables — loaded for all hosts automatically.
# Survives inventory regeneration from Terraform output.

ansible_user: ubuntu
ansible_ssh_private_key_file: ~/.ssh/id_rsa

kubernetes_version: "1.29"
pod_network_cidr: "10.244.0.0/16"
calico_version: "v3.27.3"
